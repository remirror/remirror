## API Report File for "@remirror/ui"

> Do not edit this file. It is a report generated by [API Extractor](https://api-extractor.com/).

```ts

import { Context } from 'react';
import { FC } from 'react';
import { ReactElement } from 'react';
import { RemirrorTheme } from '@remirror/core-types';
import { RemirrorThemeContextType } from '@remirror/core-types';
import { SerializedStyles } from '@emotion/core';
import { ThemeParams } from '@remirror/core-types';

// @public
export const applyColorMode: (theme: RemirrorTheme, mode: string) => RemirrorTheme;

// @public
export const baseTheme: RemirrorTheme;

// @public (undocumented)
export const cssValueUnits: (val: unknown) => string | undefined;

// @public (undocumented)
export const defaultRemirrorThemeValue: RemirrorThemeContextType;

// @public
export const EmotionThemeContext: Context<RemirrorTheme>;

// @public
export const getColorModes: (theme: RemirrorTheme) => string[];

// @public
export const getFactory: (theme: RemirrorTheme) => <GReturn = any>(path: string | (string | number)[], fallback?: any) => GReturn;

// @public
export const hasThemeProp: (val: unknown) => val is ThemeParams;

// @public
export class HSL {
    // (undocumented)
    static $$id: symbol;
    readonly a: number;
    alpha(alpha?: number | string): HSL;
    analagous(direction?: 'forwards' | 'backwards'): [HSL, HSL, HSL];
    readonly brightness: number;
    clone(hsla?: Partial<HSLObject>): HSL;
    complement(): HSL;
    static create(value: HSLCreateParams): HSL;
    darken(percent: number | string): HSL;
    desaturate(percent: number | string): HSL;
    fade(percent: number | string): HSL;
    readonly h: number;
    hue(hue: number | string): HSL;
    // (undocumented)
    readonly isDark: boolean;
    // (undocumented)
    readonly isLight: boolean;
    readonly l: number;
    lighten(percent: number | string): HSL;
    lightness(lightness: number | string): HSL;
    rectangle(spacing?: number): [HSL, HSL, HSL, HSL];
    rotate(degrees: number): HSL;
    readonly s: number;
    saturate(percent: number | string): HSL;
    saturation(saturation: number | string): HSL;
    splitComplement(): [HSL, HSL, HSL];
    square(): [HSL, HSL, HSL, HSL];
    toArray(): readonly [number, number, number, number];
    toJSON(): {
        h: number;
        s: number;
        l: number;
        a: number;
    };
    toRGB(): [number, number, number, number?];
    toString(): string;
    triadic(): [HSL, HSL, HSL];
    unfade(percent: number | string): HSL;
}

// @public (undocumented)
export type HSLCreateParams = HSL | HSLObject | NamedHSLObject | HSLTuple | string;

// @public (undocumented)
export interface HSLObject {
    // (undocumented)
    a?: number | string;
    // (undocumented)
    h: number | string;
    // (undocumented)
    l: number | string;
    // (undocumented)
    s: number | string;
}

// @public (undocumented)
export type HSLTuple = [number | string, number | string, number | string, (number | string)?];

// @public (undocumented)
export const isHSL: (value: unknown) => value is HSL;

// @public
export const isSerializedStyle: (val: unknown) => val is SerializedStyles;

// @public (undocumented)
export interface NamedHSLObject {
    // (undocumented)
    alpha?: number | string;
    // (undocumented)
    hue: number | string;
    // (undocumented)
    lightness: number | string;
    // (undocumented)
    saturation: number | string;
}

// @public
export const numberToPixels: (val: string | number) => string;

// @public
export const RemirrorThemeContext: Context<RemirrorThemeContextType>;

// @public
export const RemirrorThemeProvider: FC<RemirrorThemeProviderProps>;

// @public (undocumented)
export interface RemirrorThemeProviderProps {
    children: ReactElement;
    // Warning: (ae-forgotten-export) The symbol "DisableMerge" needs to be exported by the entry point index.d.ts
    disableMerge?: DisableMerge[];
    initialColorMode?: string;
    theme: Partial<RemirrorTheme> | ((currentTheme: RemirrorTheme) => RemirrorTheme);
    withoutEmotion?: boolean;
}

// @public
export const RemirrorThemeRoot: FC<RemirrorThemeRootProps>;

// @public (undocumented)
export interface RemirrorThemeRootProps {
    makeGlobal?: boolean;
}

// @public
export const sx: RemirrorThemeContextType['sx'];

// @public
export const useEmotionTheme: () => RemirrorTheme;

// @public
export const useRemirrorTheme: () => RemirrorThemeContextType;

// @public (undocumented)
export interface ValidHSLObject {
    // (undocumented)
    a?: number;
    // (undocumented)
    h: number;
    // (undocumented)
    l: number;
    // (undocumented)
    s: number;
}

// @public (undocumented)
export type ValidHSLTuple = [number, number, number, number?];

// @public (undocumented)
export const withoutEmotionProps: Pick<RemirrorThemeContextType, 'css' | 'sx'>;


// (No @packageDocumentation comment for this package)

```

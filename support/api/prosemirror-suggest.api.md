## API Report File for "prosemirror-suggest"

> Do not edit this file. It is a report generated by [API Extractor](https://api-extractor.com/).

```ts

import type { AnyFunction } from '@remirror/core-types';
import type { CompareStateParameter } from '@remirror/core-types';
import { DecorationSet } from 'prosemirror-view';
import type { EditorSchema } from '@remirror/core-types';
import type { EditorState } from '@remirror/core-types';
import { EditorView } from 'prosemirror-view';
import type { EditorViewParameter } from '@remirror/core-types';
import type { FromToParameter } from '@remirror/core-types';
import { noop } from '@remirror/core-helpers';
import { Plugin as Plugin_2 } from 'prosemirror-state';
import type { SelectionParameter } from '@remirror/core-types';
import type { TextParameter } from '@remirror/core-types';
import type { TransactionParameter } from '@remirror/core-types';

// @public
export interface AddIgnoredParameter extends RemoveIgnoredParameter {
    specific?: false;
}

// @public
export function addSuggester<Schema extends EditorSchema = any>(state: EditorState<Schema>, suggester: Suggester): () => void;

// @public
export enum ChangeReason {
    JumpBackward = "jump-backward-change",
    JumpForward = "jump-forward-change",
    Move = "move",
    SelectionInside = "selection-inside",
    Start = "start",
    Text = "change-character"
}

// @public
export interface CompareMatchParameter {
    next: SuggestStateMatch;
    prev: SuggestStateMatch;
}

// @public
export const createRegexFromSuggester: ({ char, matchOffset, startOfLine, supportedCharacters, }: Pick<Required<Suggester>, 'startOfLine' | 'char' | 'supportedCharacters' | 'matchOffset'>, flags?: string) => RegExp;

// @public
export interface CreateSuggestCommandParameter extends Partial<ReasonParameter>, EditorViewParameter, SuggestStateMatchParameter, SuggestMarkParameter, SuggestIgnoreParameter {
}

// @public (undocumented)
export const DEFAULT_SUGGESTER: {
    appendText: string;
    createCommand: () => typeof noop;
    ignoredClassName: any;
    ignoredTag: string;
    invalidPrefixCharacters: any;
    keyBindings: {};
    matchOffset: number;
    noDecorations: boolean;
    onChange: () => boolean;
    onCharacterEntry: () => boolean;
    onExit: () => boolean;
    startOfLine: boolean;
    suggestClassName: string;
    suggestTag: string;
    supportedCharacters: RegExp;
    validPrefixCharacters: RegExp;
};

// @public (undocumented)
export interface DocChangedParameter {
    docChanged: boolean;
}

// @public (undocumented)
export const escapeChar: (char: string) => any;

// @public
export enum ExitReason {
    End = "exit-end",
    InvalidSplit = "invalid-exit-split",
    JumpBackward = "jump-backward-exit",
    JumpForward = "jump-forward-exit",
    MoveEnd = "move-end",
    MoveStart = "move-start",
    Removed = "delete",
    SelectionOutside = "selection-outside",
    Split = "exit-split"
}

// @public
export interface FromToEndParameter extends FromToParameter {
    end: number;
}

// @public
export const getRegexPrefix: (onlyStartOfLine: boolean) => "" | "^";

// @public
export function getSuggestPluginState(state: EditorState): SuggestState;

// @public
export const isChange: (compare: Partial<CompareMatchParameter>) => compare is CompareMatchParameter;

// @public (undocumented)
export const isChangeReason: (value: unknown) => value is ChangeReason;

// @public
export const isEntry: (compare: Partial<CompareMatchParameter>) => compare is Pick<CompareMatchParameter, "next">;

// @public
export const isExit: (compare: Partial<CompareMatchParameter>) => compare is Pick<CompareMatchParameter, "prev">;

// @public
export const isExitReason: (value: unknown) => value is ExitReason;

// @public
export const isInvalidSplitReason: (value?: unknown) => value is ExitReason.InvalidSplit;

// @public
export const isJump: (compare: Partial<CompareMatchParameter>) => compare is CompareMatchParameter;

// @public
export const isJumpReason: (map: SuggestReasonMap) => map is Required<SuggestReasonMap>;

// @public
export const isMove: (compare: Partial<CompareMatchParameter>) => compare is CompareMatchParameter;

// @public
export const isRemovedReason: (value?: unknown) => value is ExitReason.Removed;

// @public
export function isSelectionExitReason(value: unknown): boolean;

// @public
export const isSplitReason: (value?: unknown) => value is ExitReason.Split;

// @public
export const isValidMatch: (match: SuggestStateMatch | undefined) => match is SuggestStateMatch<import("@remirror/core-types").AnyFunction<void>>;

// @public
export interface KeyboardEventParameter {
    event: KeyboardEvent;
}

// @public
export interface MatchValue {
    full: string;
    partial: string;
}

// @public
export interface OnKeyDownParameter extends SuggestStateMatch, EditorViewParameter, KeyboardEventParameter {
}

// @public
export interface ReasonMatchParameter<Reason> {
    match: SuggestStateMatchReason<Reason>;
}

// @public
export interface ReasonParameter<Reason = ExitReason | ChangeReason> {
    reason: Reason;
}

// @public
export const regexToString: (regexOrString: string | RegExp) => string;

// @public
export interface RemoveIgnoredParameter extends Pick<Suggester, 'char' | 'name'> {
    from: number;
}

// @public
export function removeSuggester<Schema extends EditorSchema = any>(state: EditorState<Schema>, suggester: Suggester | string): void;

// @public
export const selectionOutsideMatch: ({ match, selection, }: Partial<SuggestStateMatchParameter> & SelectionParameter) => boolean;

// @public
export function suggest<Schema extends EditorSchema = any>(...suggesters: Suggester[]): Plugin_2<SuggestState, Schema>;

// @public (undocumented)
export interface SuggestCallbackParameter<Command extends AnyFunction<void> = AnyFunction<void>> extends SuggestStateMatch, EditorViewParameter, SuggestCommandParameter<Command>, SuggestIgnoreParameter {
}

// @public (undocumented)
export type SuggestChangeHandlerMethod<Command extends AnyFunction<void> = AnyFunction<void>> = (parameter: SuggestChangeHandlerParameter<Command>) => void;

// @public
export interface SuggestChangeHandlerParameter<Command extends AnyFunction<void> = AnyFunction<void>> extends SuggestCallbackParameter<Command>, ReasonParameter<ChangeReason> {
}

// @public (undocumented)
export type SuggestCharacterEntryMethod<Command extends AnyFunction<void> = AnyFunction<void>> = (parameter: SuggestCharacterEntryParameter<Command>) => boolean;

// @public
export interface SuggestCharacterEntryParameter<Command extends AnyFunction<void> = AnyFunction<void>> extends SuggestCallbackParameter<Command>, FromToParameter, TextParameter {
}

// @public
export interface SuggestCommandParameter<Command extends AnyFunction<void> = AnyFunction<void>> {
    command: Command;
}

// @public
export interface Suggester<Command extends AnyFunction<void> = AnyFunction<void>> {
    appendText?: string;
    char: string;
    createCommand?: (parameter: CreateSuggestCommandParameter) => Command;
    ignoredClassName?: string;
    ignoredTag?: string;
    invalidPrefixCharacters?: RegExp | string;
    keyBindings?: SuggestKeyBindingMap<Command> | (() => SuggestKeyBindingMap<Command>);
    matchOffset?: number;
    name: string;
    noDecorations?: boolean;
    onChange?: SuggestChangeHandlerMethod<Command>;
    onCharacterEntry?: SuggestCharacterEntryMethod<Command>;
    onExit?: SuggestExitHandlerMethod<Command>;
    startOfLine?: boolean;
    suggestClassName?: string;
    suggestTag?: string;
    supportedCharacters?: RegExp | string;
    validPrefixCharacters?: RegExp | string;
}

// @public (undocumented)
export interface SuggesterParameter<Command extends AnyFunction<void> = AnyFunction<void>> {
    suggester: Required<Suggester<Command>>;
}

// @public (undocumented)
export type SuggestExitHandlerMethod<Command extends AnyFunction<void> = AnyFunction<void>> = (parameter: SuggestExitHandlerParameter<Command>) => void;

// @public
export interface SuggestExitHandlerParameter<Command extends AnyFunction<void> = AnyFunction<void>> extends SuggestCallbackParameter<Command>, ReasonParameter<ExitReason> {
}

// @public
export interface SuggestIgnoreParameter {
    addIgnored: (parameter: AddIgnoredParameter) => void;
    clearIgnored: (name?: string) => void;
    ignoreNextExit: () => void;
}

// @public
export type SuggestKeyBinding<Command extends AnyFunction<void> = AnyFunction<void>> = (parameter: SuggestKeyBindingParameter<Command>) => boolean | void;

// @public
export type SuggestKeyBindingMap<Command extends AnyFunction<void> = AnyFunction<void>> = Partial<Record<'Enter' | 'ArrowDown' | 'ArrowUp' | 'ArrowLeft' | 'ArrowRight' | 'Esc' | 'Delete' | 'Backspace', SuggestKeyBinding<Command>>> & Record<string, SuggestKeyBinding<Command>>;

// @public
export interface SuggestKeyBindingParameter<Command extends AnyFunction<void> = AnyFunction<void>> extends SuggestCallbackParameter<Command>, SuggestMarkParameter, KeyboardEventParameter {
}

// @public
export interface SuggestMarkParameter {
    setMarkRemoved: () => void;
}

// @public
export interface SuggestReasonMap {
    change?: SuggestStateMatchReason<ChangeReason>;
    exit?: SuggestStateMatchReason<ExitReason>;
}

// @public
export type SuggestReplacementType = 'full' | 'partial';

// @public
export class SuggestState {
    constructor(suggesters: Suggester[]);
    readonly addIgnored: ({ from, char, name, specific }: AddIgnoredParameter) => void;
    addSuggester(suggester: Suggester): () => void;
    apply({ tr, newState }: TransactionParameter & CompareStateParameter): this;
    readonly clearIgnored: (name?: string) => void;
    static create(suggesters: Suggester[]): SuggestState;
    decorations(state: EditorState): DecorationSet<any>;
    handleKeyDown(event: KeyboardEvent): boolean;
    handleTextInput({ text, from, to }: HandleTextInputParameter): boolean;
    // (undocumented)
    readonly ignoreNextExit: () => void;
    init(view: EditorView): this;
    get match(): Readonly<SuggestStateMatch> | undefined;
    get removed(): boolean;
    readonly removeIgnored: ({ from, char, name }: RemoveIgnoredParameter) => void;
    removeSuggester(suggester: Suggester | string): void;
    readonly setMarkRemoved: () => void;
    // (undocumented)
    toJSON(): Readonly<SuggestStateMatch<import("@remirror/core-types").AnyFunction<void>>>;
    viewHandler(): {
        update: any;
    };
}

// @public
export interface SuggestStateMatch<Command extends AnyFunction<void> = AnyFunction<void>> extends SuggesterParameter<Command> {
    matchText: MatchValue;
    queryText: MatchValue;
    range: FromToEndParameter;
}

// @public
export interface SuggestStateMatchParameter {
    match: SuggestStateMatch;
}

// @public (undocumented)
export interface SuggestStateMatchReason<Reason> extends SuggestStateMatch, ReasonParameter<Reason> {
}


// (No @packageDocumentation comment for this package)

```

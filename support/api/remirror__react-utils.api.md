## API Report File for "@remirror/react-utils"

> Do not edit this file. It is a report generated by [API Extractor](https://api-extractor.com/).

```ts

import type { AnyFunction } from '@remirror/core-types';
import { ComponentType } from 'react';
import { Fragment } from 'react';
import { ReactElement } from 'react';
import { ReactNode } from 'react';
import type { UnknownShape } from '@remirror/core-types';

// @public
export const addKeyToElement: (element: ReactNode, key: string | number) => any;

// @public
export const asDefaultProps: <Props extends object>() => <DefaultProps extends Partial<Props>>(props: DefaultProps) => DefaultProps;

// @public
export function getElementProps<Type = UnknownShape>(element: JSX.Element): UnknownShape & Type & {
    children: JSX.Element;
};

// @public
export function isReactDOMElement<Props extends object = any>(value: unknown): value is ReactElement<Props> & {
    type: string;
};

// @public
export function isReactFragment<Props extends object = any>(value: unknown): value is ReactElement<Props> & {
    type: typeof Fragment;
};

// @public
export const isRemirrorContextProvider: <Options extends object = any>(value: unknown) => value is RemirrorElement<Options>;

// @public
export const isRemirrorElement: <Options extends object = any>(value: unknown) => value is RemirrorElement<Options>;

// @public
export const isRemirrorProvider: <Options extends object = any>(value: unknown) => value is RemirrorElement<Options>;

// @public
export function isValidElement<Props extends object = any>(value: unknown): value is ReactElement<Props>;

// @public
export const oneChildOnly: <Props extends object = any>(value: unknown) => ReactElement<Props, string | ((props: any) => ReactElement<any, string | any | (new (props: any) => import("react").Component<any, any, any>)>) | (new (props: any) => import("react").Component<any, any, any>)>;

// @public
export const propIsFunction: (value: unknown) => value is AnyFunction<any>;

// @public (undocumented)
export interface RemirrorComponentStaticProperties {
    $$remirrorType: RemirrorType;
}

// @public (undocumented)
export type RemirrorComponentType<Props extends object = object> = ComponentType<Props> & RemirrorComponentStaticProperties;

// @public (undocumented)
export type RemirrorElement<Options extends object = any> = ReactElement & {
    type: RemirrorComponentType<Options>;
};

// @public
export enum RemirrorType {
    ContextProvider = "context-provider",
    // (undocumented)
    Editor = "editor",
    I18nProvider = "i18n-provider",
    Provider = "provider",
    // (undocumented)
    SSR = "ssr",
    ThemeProvider = "theme-provider"
}


// (No @packageDocumentation comment for this package)

```

---
title: of
---

The `of` keyword is used a lot in the codebase as a way of creating an instance
instead of using the `new` keyword.

```ts
const manager = ExtensionManager.of([...extensions]);
```

So for all the class constructors exposed by the library the constructor is
intentionally kept private.

---
title: of
---

The `of` keyword is used a lot in the codebase as a way of creating an instance instead of using the
`new` keyword.

```ts
const manager = Manager.of([...extensions]);
```

So for all the class constructors exposed by the library the constructor is intentionally kept
private.

<!-- Do not edit this file. It is automatically generated by API Documenter. -->

[Home](./index.md) &gt; [@remirror/react-hooks](./react-hooks.md) &gt; [useEffectOnUpdate](./react-hooks.useeffectonupdate.md)

## useEffectOnUpdate variable

React effect hook that ignores the first invocation (e.g. on mount).

<b>Signature:</b>

```typescript
useEffectOnUpdate: typeof useEffect
```

## Remarks

The signature is exactly the same as the useEffect hook.

```tsx
import React from 'react'
import {useEffectOnUpdate} from 'react-use';

const Demo = () => {
  const [count, setCount] = React.useState(0);

  React.useEffect(() => {
    const interval = setInterval(() => {
      setCount(count => count + 1)
    }, 1000)

    return () => {
      clearInterval(interval)
    }
  }, [])

  useEffectOnUpdate(() => {
    log('count', count) // will only show 1 and beyond

    return () => { // *OPTIONAL*
      // do something on unmount
    }
  }) // you can include deps array if necessary

  return <div>Count: {count}</div>
};

```


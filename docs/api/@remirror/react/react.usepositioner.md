<!-- Do not edit this file. It is automatically generated by API Documenter. -->

[Home](./index.md) &gt; [@remirror/react](./react.md) &gt; [usePositioner](./react.usepositioner.md)

## usePositioner variable

Hooks to receive the positioner props and add them to your component

```ts
import { bubblePositioner } from '@remirror/react';

const MenuComponent: FC = () => {
  const { isActive, bottom, left } = usePositioner({positionerId: 'bubbleMenu', positioner: bubblePositioner })

  return (
    <div style={{ bottom, left }}>
      <MenuIcon {...properties} />
    </div>
  );
}

<RemirrorProvider extensions={[]}>
  <MenuComponent />
</RemirrorProvider>

```

<b>Signature:</b>

```typescript
usePositioner: <GRefKey extends string = "ref">({ positioner, ...rest }: UsePositionerParams<GRefKey>) => import("../react-types").GetPositionerReturn<GRefKey>
```
